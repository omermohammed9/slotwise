# API origin used by the Vite frontend during local development and builds.
VITE_SLOTWISE_API_BASE_URL=http://localhost:3000

# Must match SLOTWISE_CSRF_COOKIE_NAME when the backend overrides its default.
VITE_SLOTWISE_CSRF_COOKIE_NAME=slotwise_csrf
